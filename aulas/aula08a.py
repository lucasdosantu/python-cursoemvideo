from math import sqrt
import random
import datetime
# Da biblioteca Math, importe apenas Sqrt (raíz quadrada)

num = random.randint(1, 100)
raiz = sqrt(num)

print(f'A raiz de {num} é {raiz:.2f}.\n')
